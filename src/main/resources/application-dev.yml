spring:
  application:
    name: erp-api
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://erp_database:5432/erp
    username: root
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: none
    open-in-view: off
  graphql:
    graphiql:
      enabled: true
    cors:
      allowed-origins: "*"
  servlet:
    multipart:
      max-request-size: 10485760
      max-file-size: 10485760

file-extensions-config:
  allowed-extensions: jpg,jpeg,png,bmp
