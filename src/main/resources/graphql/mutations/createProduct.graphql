mutation {
    createProduct(
        input: {name: "", supplierId: 0, price: 0.00, unit: "", onStock: 0}
    ) {
        id
        name
        supplier {
            id
            name
            phone
            email
        }
        price
        onStock
        unit
        photo
    }
}
