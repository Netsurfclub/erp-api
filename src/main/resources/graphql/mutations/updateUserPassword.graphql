mutation {
    updateUserPassword(
        input: {userId: 0, currentPassword: "", newPassword: "", confirmNewPassword: ""}
    ) {
        id
        username
        firstName
        lastName
        email
    }
}
